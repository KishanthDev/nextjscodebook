// page.tsx
import BubblePreview from '../../../components/preview/BubblePreview';

export default function Page({defaultSettings}: any) {

  return <BubblePreview defaultSettings={defaultSettings} />;
}
