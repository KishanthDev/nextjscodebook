// page.tsx
import ChatContactPreview from '../../../components/preview/ChatContactPreview';

export default function Page({defaultSettings}: any) {

  return <ChatContactPreview defaultSettings={defaultSettings} />;
}
